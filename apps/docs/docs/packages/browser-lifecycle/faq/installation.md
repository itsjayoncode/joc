# Installation FAQ

## Which package name should I use?

Use `@jayoncode/browser-lifecycle`. Older construction docs may reference `@jayoncode/browser-session`.

## Can I use it from the monorepo without publishing?

Yes. Build packages with `pnpm build:packages` and import from the workspace.

## Related documentation

[Installation](/packages/browser-lifecycle/installation)
