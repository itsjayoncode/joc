# API

The API section is reserved for future package reference material.

## Planned scope

- generated API references where appropriate
- hand-written conceptual API pages where generated output is not enough
- shared reference conventions across the JOC ecosystem

Until packages are implemented, this section stays intentionally lightweight.
