# Lifecycle Playground

Route: `/lifecycle`

Demonstrates page lifecycle normalization via `src/lib/playground-lifecycle.ts`.

- Freeze/resume/sleep/wake panels derived from `page:suspend`, `page:resume`, and visibility events
- Browser compatibility matrix
- Live lifecycle event stream
