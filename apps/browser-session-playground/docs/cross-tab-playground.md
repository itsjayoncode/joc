# Cross Tab Playground

Route: `/cross-tab`

Demonstrates cross-tab coordination via `src/lib/playground-cross-tab.ts`.

- Primary/secondary tab role from `tab:primary` / `tab:secondary`
- Live `tab:message` communication monitor
- Leader election timeline via heartbeats
- Open multiple tabs to observe failover
