# Apps

This directory contains first-party applications that support the JOC ecosystem.

Current applications include:

- `docs` for official VitePress documentation
- `playground` for interactive local package validation
- `browser-session-playground` for the long-lived Browser Lifecycle engineering shell
- `website` reserved for the future public project presence

These apps are designed to support the ecosystem without containing package feature implementations themselves.
