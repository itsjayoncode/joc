# Tests

This directory contains shared repository-level testing assets for the JOC monorepo.

## Purpose

- `fixtures/` stores stable inputs used across future test suites.
- `mocks/` stores reusable mocks and stubs for package testing.
- `helpers/` stores low-level utilities that are specific to tests.

Phase 1.2 establishes the structure only. No feature tests are included yet.
