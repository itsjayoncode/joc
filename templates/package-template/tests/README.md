# Package Tests

Test layout for every JOC package:

- `unit/` for isolated behavior
- `integration/` for multi-module or environment-level behavior
- `fixtures/` for stable sample inputs
- `helpers/` for test-only utilities
