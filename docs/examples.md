# Framework Examples

Official Browser Lifecycle framework examples live under `examples/` in the monorepo.

Each example demonstrates:

- installation
- initialization
- configuration
- event subscriptions
- cleanup

Use `createBrowserLifecycle()` from `@jayoncode/browser-lifecycle` in every example.
