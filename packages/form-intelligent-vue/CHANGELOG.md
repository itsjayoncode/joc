# Changelog

## 0.1.0

Initial release of the Vue adapter for `@jayoncode/form-intelligent`.

### Added

- `useForm()`, `useFormState()`, `useField()`, and `provideForm()` composables
- Reactive `form.state` ref with automatic destroy on scope dispose
