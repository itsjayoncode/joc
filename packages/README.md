# Packages

This directory contains the JOC library ecosystem.

Public packages (`browser-lifecycle`, `form-intelligence`, `object-diff`) are independently installable on npm. `@jayoncode/form-intelligent` remains as a compatibility re-export. Framework and schema adapters live beside their parent package.
