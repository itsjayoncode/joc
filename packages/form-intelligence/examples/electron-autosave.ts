/**
 * Planned — Electron autosave recipe.
 * Core pattern is already in wizard-workflow.ts (`workflow.autosave` + `draft`).
 * Pair with `createBrowserLifecyclePlugin` from `@jayoncode/form-intelligence/plugins`.
 */
export {};
